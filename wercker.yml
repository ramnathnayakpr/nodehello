box: node
build:
  steps:
    - npm-install:
        package: express
#    - internal/watch:
#        code: node nodehello.js
#        reload: true
    - internal/docker-push: 
        image-name: nodehello
        username: $USERNAME
        password: $PASSWORD
        registry: https://hub.docker.com
        repository: $USERNAME/nodehello
        tag: latest