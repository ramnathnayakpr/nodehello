box: node
build:
  steps:
    - npm-install:
        package: express
#    - internal/watch:
#        code: node nodehello.js
#        reload: true
    - internal/docker-push: 
        username: $USERNAME
        password: $PASSWORD
        registry: https://hub.docker.com/ramnathn
        repository: $USERNAME/nodehello
        tag: latest
